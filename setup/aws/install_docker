#/bin/bash

#
# apt install -y curl
#
# curl https://raw.githubusercontent.com/WALL-E/static/master/setup/aws/install_docker | bash
#
# install docker to AWS Linux 2
#


role=`id -u`
if test $role -ne 0
then
    echo "You install Harbor which requires root privileges"
    exit 1
fi
